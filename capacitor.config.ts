import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rebotgoclient',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
